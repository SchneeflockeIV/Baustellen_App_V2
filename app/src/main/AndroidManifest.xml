<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="comschneeflockeivbaustellen_app_v2.github.baustellen_app_v2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AccErstellen" />
        <activity android:name=".PassVergessen" />
        <activity android:name=".BaustellenViewActivity" />
        <activity
            android:name=".BaustellenViewActivity2"
            android:label="@string/title_activity_baustellen_view2"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MaterialViewActivity"
            android:label="@string/title_activity_material_view"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".BaustelleErstellen" />
        <activity android:name=".MaterialErstellen"></activity>
    </application>

</manifest>